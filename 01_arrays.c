#include <stdio.h>

int main()
{

    int marks[5];      // reserve space to store 90 integers

     marks [0] = 85;
  marks [1] = 45;
//  we can go all the way till marks [89]
    printf(" marks 0 : %d\n marks 1 : %d\n", marks[0], marks[1]);
    return 0;
}
